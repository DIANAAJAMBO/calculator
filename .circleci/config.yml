version: 2
jobs:
  build:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run: yarn install  # Or npm install

      # Configure the test command
      - run:
          name: Run Tests
          command: |
            yarn test
            # Or if you use npm:
            # npm test